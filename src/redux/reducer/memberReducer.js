// import { CREATE_PROJECT } from "../../util/setting"

const initialState = {
  members: []
}

export default (state = initialState, action) => {
  switch (action.type) {

    default:
      return state
  }
}
